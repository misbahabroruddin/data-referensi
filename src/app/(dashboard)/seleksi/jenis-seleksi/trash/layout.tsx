export function generateMetadata() {
  return { title: "Trash Jenis Seleksi | Data Referensi - UNSIA" };
}

export default function TrashJenisSeleksiLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return <>{children}</>;
}
