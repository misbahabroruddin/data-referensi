export function generateMetadata() {
  return { title: "Trash Komposisi | Data Referensi - UNSIA" };
}

export default function TrashKomposisiLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return <>{children}</>;
}
