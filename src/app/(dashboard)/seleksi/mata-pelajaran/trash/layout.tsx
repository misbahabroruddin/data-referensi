export function generateMetadata() {
  return { title: "Trash Mata Pelajaran | Data Referensi - UNSIA" };
}

export default function TrashMataPelajaranLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return <>{children}</>;
}
