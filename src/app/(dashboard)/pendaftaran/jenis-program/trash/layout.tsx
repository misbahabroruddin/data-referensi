export function generateMetadata() {
  return { title: "Trash Jenis Program | Data Referensi - UNSIA" };
}

export default function TrashJenisProgramLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return <>{children}</>;
}
