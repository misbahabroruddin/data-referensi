export function generateMetadata() {
  return { title: "Trash Jalur Pendaftaran | Data Referensi - UNSIA" };
}

export default function TrashJalurPendaftaranLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return <>{children}</>;
}
