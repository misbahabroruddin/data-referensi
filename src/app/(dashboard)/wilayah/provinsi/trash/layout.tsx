export function generateMetadata() {
  return { title: "Trash Provinsi | Data Referensi - Unsia" };
}

export default function ProvinsiLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return <>{children}</>;
}
