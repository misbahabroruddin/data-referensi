export function generateMetadata() {
  return { title: "Trash Pengumuman | Data Referensi - UNSIA" };
}

export default function TrashPengumumanLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return <>{children}</>;
}
