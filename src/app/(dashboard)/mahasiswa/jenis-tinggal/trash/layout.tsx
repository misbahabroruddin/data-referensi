export function generateMetadata() {
  return { title: "Trash Jenis Tinggal | Data Referensi - UNSIA" };
}

export default function TrashJenisTinggalLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return <>{children}</>;
}
