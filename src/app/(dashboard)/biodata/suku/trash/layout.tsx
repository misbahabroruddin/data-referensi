export function generateMetadata() {
  return { title: "Trash Suku | Data Referensi - UNSIA" };
}

export default function TrashSukuLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return <>{children}</>;
}
