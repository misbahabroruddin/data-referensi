export function generateMetadata() {
  return { title: "Trash Penghasilan | Data Referensi - UNSIA" };
}

export default function TrashPenghasilanLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return <>{children}</>;
}
