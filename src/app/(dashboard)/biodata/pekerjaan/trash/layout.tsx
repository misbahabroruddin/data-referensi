export function generateMetadata() {
  return { title: "Trash Pekerjaan | Data Referensi - UNSIA" };
}

export default function TrashPekerjaanLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return <>{children}</>;
}
