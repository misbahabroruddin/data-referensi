export function generateMetadata() {
  return { title: "Trash Agama | Data Referensi - UNSIA" };
}

export default function TrashAgamaLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return <>{children}</>;
}
