export function generateMetadata() {
  return { title: "Trash Ukuran Almamater | Data Referensi - UNSIA" };
}

export default function TrashUkuranAlmamaterLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return <>{children}</>;
}
