export function generateMetadata() {
  return { title: "Ukuran Almamater" };
}

export default function UkuranAlmamaterLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return <>{children}</>;
}
