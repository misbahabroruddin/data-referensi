export function generateMetadata() {
  return { title: "Trash Program | Data Referensi - UNSIA" };
}

export default function TrashProgramLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return <>{children}</>;
}
